<main class="main">
  <div class="inner">
      <% if user_signed_in? %>
        <%= link_to "#{current_user.name}さん,今日も学習がんばりましょう！", user_path(current_user.id), class: :greeting__link %>
      <% end %>
    <div class="card__wrapper">
    </div>
  </div>
</main>