<div class="card">
  <%= link_to image_tag(forms.image, class: :card__img ), form_path(forms.id) %>
  <div class="card__body">
    <%= link_to forms.day, form_path(forms.id), class: :card__title %>
    <%= link_to forms.user.name, user_path(forms.user.id), class: :card__user %>
  </div>
</div>